# pi-prompt-history

Ctrl+R prompt history picker for pi.

- Searches prompts across all projects by default.
- Press Ctrl+S in the picker to cycle scope: all projects → current project → current session.
- Press Enter to replace the editor text with the selected prompt.
- Press Escape to cancel.

## Development

```bash
pi -e ./packages/pi-prompt-history
```
